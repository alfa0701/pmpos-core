import { CardTypeRecord } from '../../models';

const data = {
    id: 'HJJK7P6cG',
    name: 'Products',
    reference: 'Product'
};

// id: string;
// name: string;
// reference: string;
// displayFormat: string;
// commands: string[];
// tagTypes: string[];
// subCardTypes: string[];

export default new CardTypeRecord(data);
